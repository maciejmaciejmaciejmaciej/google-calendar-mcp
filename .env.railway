# Railway Environment Configuration for Google Calendar MCP Server

# Transport mode - MUST be HTTP for Railway deployment
TRANSPORT=http

# Server configuration - Railway will provide PORT via environment
PORT=${PORT:-3000}
HOST=0.0.0.0

# Production settings
NODE_ENV=production
DEBUG=false

# OAuth credentials path
GOOGLE_OAUTH_CREDENTIALS=./gcp-oauth.keys.json

# Optional: Custom token storage (Railway provides persistent storage)
# GOOGLE_CALENDAR_MCP_TOKEN_PATH=/app/tokens
